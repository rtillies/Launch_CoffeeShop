@model List<Customer>

<h1>Customers</h1>

@if (Model.Count > 0)
{
	@foreach (var customer in Model)
	{
		<p><a href="/customers/details/@customer.Id">@customer.Name</a></p>
	}
}
else
{
	<p>No customers to display.</p>
}